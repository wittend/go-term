I need to create a project that runs from the commandline and implements a terminal window that behaves like a traditional terminal such as gnome-terminal.  
- I Want the background processing to be handled by go. 
- I want the terminal window to support basic terminal commands such as ls, cd, and pwd.
- I want the terminal window to be surrounded by a border.
- I want the terminal window to have a customizable color scheme.
- I want the terminal window to support copy and paste functionality.
- I want the terminal window to support tab completion for commands and file paths.
- I want the terminal window to support syntax highlighting for common programming languages.
- I want the terminal window to support mouse scrolling and navigation.
- I want the terminal window to support keyboard shortcuts for common actions.
- I want the terminal window to support multiple tabs for separate command sessions.
- I want the terminal window to support command history and search functionality.
- I want the terminal window to support command line argument parsing for custom configurations.
- I want the terminal window to support custom themes and font settings.
- I want the terminal window to support remote access via SSH or other protocols.
- I want the terminal window to support file transfer and editing functionality.
- I want the terminal window to handle resizing gracefully.
- I want the terminal window to support multiple terminal windows.
- I want the terminal window to support accessibility features such as screen reader support.
- I want the terminal window to support integration with popular command-line tools and utilities.
- I want the terminal window to support seamless integration with other applications and services.
- I want the terminal window to be responsive and adaptable to different screen sizes and resolutions.
- I want the terminal window to support advanced features such as command autocompletion and fuzzy search.
- I want the terminal window to support custom keybindings for common actions.
- I want the terminal window to support session capture and restoration.
- I want captured sessions to be saved to human readable files for future editing.
The project should be written in Go, though plugins or extensions can be written in any language.
- I want the project to be open source and available on GitHub.
- I want the project to have a modular architecture for easy customization and extension.
- I want the project to have comprehensive documentation and tutorials for users and developers.
- I want the project to have a robust testing suite to ensure stability and reliability.
- I want the project to have a user-friendly interface with intuitive controls and visual feedback.
- I want the project to be cross-platform compatible with Linux, macOS, and and BSD systems.
- I want the project to have a strong focus on performance and efficiency, with minimal resource usage.
- I want the project to have a vibrant and active community of users and contributors.
- I want the project to have a clear roadmap for future development and feature additions.
- I want the project to have a clear and concise license for open source contributions and usage.

I want you to review where we left off with this project and consider the these additional requirements:
1)  resizeable, non overlapping windows selectable with a tab bar.
2) A simple toolbar that respects the currently selected terminal.
3) a context sensitive status bar at the bottom that indicates the current line, character position name of currently selected terminal.
